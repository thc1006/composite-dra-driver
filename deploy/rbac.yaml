apiVersion: v1
kind: ServiceAccount
metadata:
  name: composite-dra-driver
  namespace: composite-dra-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: composite-dra-driver
rules:
  # Read underlying ResourceSlices for synthesis
  - apiGroups: ["resource.k8s.io"]
    resources: ["resourceslices"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  # Create/delete shadow ResourceClaims + update their status
  - apiGroups: ["resource.k8s.io"]
    resources: ["resourceclaims"]
    verbs: ["get", "list", "watch", "create", "update", "delete"]
  - apiGroups: ["resource.k8s.io"]
    resources: ["resourceclaims/status"]
    verbs: ["get", "update", "patch"]
  # status.allocation/reservedFor need resourceclaims/binding on 1.36 (granular status authz, beta on)
  - apiGroups: ["resource.k8s.io"]
    resources: ["resourceclaims/binding"]
    verbs: ["update", "patch"]
  # Read nodes for kubelet plugin registration
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: composite-dra-driver
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: composite-dra-driver
subjects:
  - kind: ServiceAccount
    name: composite-dra-driver
    namespace: composite-dra-system
